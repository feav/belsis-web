<form method="post" action="{{ path('sortie_caisse_delete', {'id': sortie_caisse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie_caisse.id) }}">
    <button class="btn">Delete</button>
</form>
