<form method="post" action="{{ path('mode_paiement_delete', {'id': mode_paiement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mode_paiement.id) }}">
    <button class="btn">Delete</button>
</form>
